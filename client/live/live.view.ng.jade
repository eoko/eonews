ion-view
    ion-nav-title Flux
    ion-nav-buttons(side="right")
        button.button-clear(ng-click="toggleUserMenu()")
            i.icon.ion-ios-gear-outline
    ion-side-menus.eon-live
        ion-side-menu(side="right")
            //ion-header-bar.bar-positive
            //    .buttons
            //        //button.button.icon.ion-navicon-round(menu-close="")
            //    h1.title &nbsp;
            //    .buttons
            //        //button.button Éditer
            ion-content
                header.image
                    img(src='assets/images/circle_rocketx2.png')
                    p.user @{{user.username}}
                    div.article-meter-ct
                        meter(max="{{stats.total}}" value="{{stats.read}}")
                        span.stats-read {{stats.read}} lu{{stats.read > 1 ? 's' : ''}}
                        span.stats-total {{stats.total}} article{{stats.total > 1 ? 's' : ''}}
                h2.title Filtres
                .list.tags
                    ion-checkbox(
                        ng-repeat="tag in tags"
                        ng-model="tagChecked[tag._id]"
                        class='{{tag.style}}') {{tag.text}}
            ion-footer-bar.bar-assertive
                h1.title &nbsp;
                .buttons
                    button.button(ng-click="logout()") Se déconnecter
        ion-side-menu-content
            //ion-header-bar.bar-positive(align-title="center")
            //    //.buttons
            //    //    button.button-clear.icon.ion-navicon-round(menu-toggle="left")
            //    h1.title Flux
            //    .buttons
            //        .button.button-clear.user(menu-toggle="right") {{user.username}}
            //        //button.button-clear.icon.ion-ios-person-outline(menu-toggle="right")
            //
            ////header
            ////    .list.list-inset
            ////        .item.item-input
            ////            i.icon.ion-search.placeholder-icon
            ////            input(type="text" placeholder="Search")
            ion-content
                //.row
                    //form(ng-submit='save()', name='form', novalidate='')
                    //    .list
                    //        label.item.item-input
                    //            input(type='text', placeholder='Name', ng-model='newThing.name')
                    //    button.button.button-block.button-positive(type='submit') Add
                ion-list.articles
                    ion-item.item-icon-right(
                            ng-repeat='post in posts' 
                            ui-sref="article({id: post._id})"
                            ng-class="{unread: !isRead(post)}")
                        .date {{post.date|date}}
                        h2 {{post.title}}
                        p.abstract {{post|abstract}}
                        div.tag-pills
                            div.button.button-small.button-outline(
                                ng-repeat="tag in post.tags"
                                class='button-{{tag.style}}') {{tag.text}}
                        i.icon.ion-ios-arrow-right.item-icon
                //.row
                //p Total number of things: {{things.length}}

pre {{posts|json}}
pre {{things|json}}
